name: MÁY CHỦ PREMIUM CỦA HEROGOODBOYVN

on:
  workflow_dispatch:
    inputs:
      run_hours:
        description: 'Chọn thời gian chạy (1-6 giờ)'
        required: true
        type: choice
        options: ['1', '2', '3', '4', '5', '6']
        default: '3'

jobs:
  premium-rdp:
    runs-on: windows-latest
    timeout-minutes: 390  # Max 6 giờ + dư

    steps:
      - name: Tải Ngrok
        run: |
          Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip -DestinationPath .
          .\ngrok.exe authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

      - name: Bật RDP + Tạo User + Tối Ưu Mượt Max
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

          $user = "HEROGOODBOYVN"
          \( pass = " \){{ secrets.RDP_PASSWORD || 'Nguyen2009' }}"

          if (-Not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name $user -Password (ConvertTo-SecureString $pass -AsPlainText -Force) -FullName "HEROGOODBOYVN Premium"
            Add-LocalGroupMember -Group "Administrators" -Member $user
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member $user
          } else {
            Set-LocalUser -Name $user -Password (ConvertTo-SecureString $pass -AsPlainText -Force)
          }

          powercfg -setactive SCHEME_MIN
          Set-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize' -Name "EnableTransparency" -Value 0
          Set-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced' -Name "TaskbarAnimations" -Value 0
          Stop-Service SysMain, DiagTrack -Force -ErrorAction SilentlyContinue

      - name: Khởi Động Tunnel & Thông Tin Kết Nối Đẹp Lung Linh
        run: |
          Start-Process -WindowStyle Hidden -FilePath ".\ngrok.exe" -ArgumentList "tcp 3389"

          Write-Host ""
          Write-Host "🌌✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨🌌" -ForegroundColor Cyan
          Write-Host "      🔥 MÁY CHỦ PREMIUM HEROGOODBOYVN ONLINE! 🔥      " -ForegroundColor Magenta
          Write-Host "   👑 Thời gian chạy: ${{ inputs.run_hours }} giờ siêu mượt! 👑   " -ForegroundColor Yellow
          Write-Host "🌌✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨✨🌌" -ForegroundColor Cyan
          Write-Host ""

          Write-Host "   Đang chờ ngrok khởi động tunnel (retry mạnh để fix lỗi connection refused)..."

          Start-Sleep -Seconds 60

          $addr = ""
          for ($i = 1; $i -le 10; $i++) {
            try {
              $tunnel = Invoke-RestMethod http://localhost:4040/api/tunnels -ErrorAction Stop
              $addr = ($tunnel.tunnels | Where-Object proto -eq "tcp").public_url -replace "tcp://",""
              if ($addr) { 
                Write-Host "   Lấy address thành công sau $i lần retry!"
                break 
              }
            } catch {
              Write-Host "   Retry lần $i - ngrok đang khởi động..."
            }
            Start-Sleep -Seconds 30
          }

          if (-not $addr) {
            Write-Host "LỖI: Không lấy được address sau 10 lần - Token ngrok sai hoặc hết hạn! Vào dashboard.ngrok.com lấy token mới!"
            exit 1
          }

          Write-Host ""
          Write-Host "   🎮 THÔNG TIN KẾT NỐI PREMIUM (copy nguyên dòng dưới): 🎮"
          Write-Host "   $addr"
          Write-Host ""
          Write-Host "   Username: HEROGOODBOYVN"
          Write-Host "   Password: Nguyen2009"
          Write-Host ""
          Write-Host "   Dùng Microsoft Remote Desktop / Windows App trên điện thoại hoặc PC"
          Write-Host "   Nếu cảnh báo chứng chỉ → Nhấn Continue là vào ngay!"
          Write-Host ""
          Write-Host "        🎉 CHÚC MỌI NGƯỜI CHƠI WINDOWS VUI VẺ! 🎉" -ForegroundColor Magenta
          Write-Host "                   Powered by HEROGOODBOYVN Premium ✨🚀"
          Write-Host ""

          Start-Sleep -Seconds ${{ inputs.run_hours * 3600 }}
